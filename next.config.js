/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}
// next.config.js
// const withTM = require('next-transpile-modules')(['components/radial']); // pass the modules you would like to see transpiled

// module.exports = withTM({});



module.exports = nextConfig
